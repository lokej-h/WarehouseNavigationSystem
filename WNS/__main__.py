"""

This is config and setup work to prepare the environment for the other modules to run.

Since there is no setup work (and the "main" code is in "main.py"), we only
import config.

"""
from . import config
